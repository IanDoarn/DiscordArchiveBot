bot:
  command_prefix: "!<"
  owner_discord_name: "baxterthehusky#1003"
  errors:
    message:
      prefix: "oopsie whoopsie~ a fucky wucky happened!"
  commands:
  # Commands go here
   - name: default
     module: "commands.default.default"
     config: "commands\\default\\config.yaml"
   - name: dummy
     module: "commands.testing.dummy"
     config: "commands\\testing\\config.yaml"

archive:
  message:
    limit: 1000000
